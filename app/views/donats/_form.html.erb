<%= form_with(model: donat, local: true) do |form| %>
  <% if donat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donat.errors.count, "error") %> prohibited this donat from being saved:</h2>

      <ul>
      <% donat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :picture_url %>
    <%= form.text_field :picture_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
